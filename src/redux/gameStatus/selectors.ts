export const selectGameId = (status: any) => status.gameStatus.matchId;
export const selectPosition = (status: any) => status.gameStatus.position;
export const selectFigure = (status: any) => status.gameStatus.figure;
export const selectIcons = (status: any) => status.gameStatus.boardState;
export const selectState = (status: any) => status.gameStatus;
